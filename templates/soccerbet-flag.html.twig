{#
/**
 * @file
 * Hilfsmakro für die Flaggen-Darstellung (circle-flags, kapowaz/circle-flags).
 *
 * Verwendung in anderen Templates:
 *   {% import 'modules/custom/soccerbet/templates/soccerbet-flag.html.twig' as flag_helper %}
 *   {{ flag_helper.flag('AUT', 'Österreich') }}
 *   {{ flag_helper.flag('ENG', 'England') }}
 *
 * Alle Flags sind SVG-Kreise (512×512), Dateiname = alpha-3 uppercase.
 * Basis-Pfad: /modules/custom/soccerbet/images/flags/svg/{code}.svg
 */
#}

{% macro flag(code, alt, width) %}
  {% if code %}
    {% set size = width|default(24) %}
    {% set src = '/modules/custom/soccerbet/images/flags/svg/' ~ code ~ '.svg' %}
    <img
      src="{{ src }}"
      alt="{{ alt|default(code)|escape }}"
      width="{{ size }}"
      height="{{ size }}"
      class="soccerbet-flag"
      loading="lazy"
    >
  {% endif %}
{% endmacro %}
